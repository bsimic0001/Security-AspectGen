		pointcut xss_<pointcut_name>(<pointcut_param>) : 
			execution(* * <function_name>(<function_params>)) && 
			(<within_string>) && 
			args(<args_string>);
			
		Object around(<pointcut_param>) : xss_<pointcut_name>(<pointcut_vars>){
			System.out.println(<advice_logic>);
			proceed(<advice_proceed>);
			return null;
		}